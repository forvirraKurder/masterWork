# Import your functions to be tested
from newton_raphson import solve  # Just an example

# Write three tests to demonstrate your knowledge of the implemented method


def test_one():
    solve()
    assert(False)

    # The final assertion should be True, to pass the test
    # e.g. A symmetry check on Ybus
    # assert(np.all(np.abs(a-a.T) < 0.0001))


def test_two():
    assert(False)


def test_three():
    assert(False)
